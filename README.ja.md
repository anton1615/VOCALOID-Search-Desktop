# VOCALOID Search Desktop

ニコニコ動画のVOCALOID動画を検索できる、モダンなSpotify風UIのローカルファースト・デスクトップアプリケーション。

> **[ニコニコ超検索](https://gokulin.info/search/) に触発されました** – 人気のニコニコ動画検索サービス。

これは [ウェブ版VOCALOID Search](https://github.com/anton1615/VOCALOID-Search) をTauriでローカルファーストのデスクトップアプリとして移植したものです。

> **注**: このプロジェクトは完全に **vibe coding** で構築されました – AIアシストによる反復開発で作成されています。

**[English](./README.md) | [中文](./README.zh.md)**

---

## スクリーンショット

![検索インターフェース](./screenshot1.png)
![PiPウィンドウ](./screenshot2.png)

---

## 機能

- **モダンなインターフェース**: Spotify風のUI、プレイリスト形式のレイアウト
- **ライト/ダークモード**: テーマ切替対応
- **多言語対応**: 英語、日本語、中国語（繁体字）
- **PiPウィンドウ**: 常に最前面に表示されるピクチャー・イン・ピクチャープレイヤー
- **視聴履歴**: 視聴した動画の記録
- **視聴済み除外**: 検索結果から既に見た動画を除外
- **ウィンドウ状態の保存**: ウィンドウ位置、サイズ、最大化状態をセッション間で記憶
- **ローカルデータベース**: 動画データをローカルに保存し、オフラインで高速検索
- **カスタム数式での並べ替え・フィルタリング**: 再生数、マイリスト数、コメント数、いいね数を独自の重み付けでソート
- **オートスキップ**: 動画のエンディングを自動スキップ（エンドロールやスポンサー部分のスキップに便利）
- **埋め込みプレイヤー**: 公式ニコニコ埋め込みプレイヤーによる連続再生
- **キーワード+タグ検索**: タグフィルタ付き全文検索
- **無限スクロール**: 固定ページネーションではなく動的読み込み

---

## ウェブ版とデスクトップ版の比較

| 機能 | ウェブ版 | デスクトップ版 |
|------|---------|---------------|
| **デプロイ** | セルフホストサーバー (NixOS/Linux) | ローカルアプリ (Windows) |
| **稼働** | 24時間サーバー運用 | オンデマンド起動 |
| **スクレイパー** | systemdタイマーで自動実行 | 手動実行 |
| **マルチユーザー** | 対応（ユーザー登録可能） | 単一ユーザー、ローカルデータ |
| **モバイル対応** | PWA/TWAでAndroid対応 | 非対応 |
| **PiPモード** | コンパクトウィンドウにリサイズ | ネイティブ常時最前面ウィンドウ |
| **データ保存** | サーバー側データベース | ローカルファイルシステム |
| **オフライン検索** | サーバー接続が必要 | 完全ローカル |
| **プラットフォーム** | NixOS/Linux限定 | Windows (Linux対応予定) |

### どちらを選ぶべき？

**ウェブ版が適している場合:**
- 24時間自動スクレイピングを希望
- マルチユーザー対応が必要
- PWAでモバイルアクセスを希望
- NixOS/Linuxサーバーを運用している

**デスクトップ版が適している場合:**
- ネイティブデスクトップ体験を希望
- OSと密結合したPiPウィンドウを希望
- サーバー管理をしたくない
- 完全オフライン検索が必要

---

## 技術仕様

| レイヤー | 技術 |
|---------|------|
| **フロントエンド** | TypeScript, Vue 3, Vite, Tailwind CSS |
| **バックエンド** | Rust (Tauri 2.x) |
| **データベース** | SQLite (FTS5 全文検索) |
| **データソース** | [ニコニコ Snapshot API v2](https://site.nicovideo.jp/search-api-docs/snapshot.html), [GetThumbInfo API](https://site.nicovideo.jp/search-api-docs/thumb-info.html) |

### 実装アーキテクチャ

```
┌─────────────────────────────────────────┐
│           Vue 3 Frontend                │
│  (Search UI, Player, Settings, PiP)     │
└─────────────────┬───────────────────────┘
                  │ Tauri IPC
┌─────────────────▼───────────────────────┐
│           Rust Backend                   │
│  (SQLite, HTTP Client, File System)     │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│    Local SQLite Database (FTS5)         │
└─────────────────────────────────────────┘
```

### 主な技術的詳細

- **カスタムプロトコル**: HTTP localhostの代わりにTauriのカスタムプロトコル（`tauri://localhost`）を使用。ニコニコ埋め込みプレイヤーのドメイン制限を回避
- **FTS5全文検索**: SQLite FTS5でAND/OR/NOT演算子を使った高速なキーワード・タグ検索を実現
- **数式ベースのスコアリング**: 独自の重み付けで柔軟な並べ替えが可能

## 動作環境

- **OS**: Windows 10/11 (x64)
- **メモリ**: 最低4GB、推奨8GB
- **ストレージ**: 実行ファイル約10MB。データベースサイズは同期範囲による（例：VOCALOID キーワード、20日間、音楽カテゴリ ≈ 40MB）。WebView2 Runtime は通常 Windows 10/11 にプリインストール済み。
- **ネットワーク**: 動画再生とデータ同期にインターネット接続が必要

## データ保存先

データベースと設定は以下の場所に保存されます：
```
Windows: %APPDATA%\com.vocaloid-search.desktop
```

### ポータブルモード

ポータブルモード（データをアプリケーションフォルダに保存）を使用するには、実行ファイルと同じディレクトリに`data/`フォルダを作成してください：
```
<vocaloid-search-desktop.exeの場所>/data/
```

ポータブルモードが有効な場合、すべてのデータ（データベース、設定、サムネイル）がこのフォルダに保存されます。フォルダごと別のコンピュータにコピーすれば、同じデータを使用できます。

**注意**: ポータブルモードと標準モードの切り替えでは、データは自動的に移行されません。

---

## 使い方

### 利用の流れ

1. **起動時チェック**: アプリ起動時、データベースが空か古いかを自動チェックします。ニコニコSnapshot APIは毎日JST 5-6時頃に更新されます。データベースが古い場合、同期を促すメッセージが表示されます。

2. **データ同期**: 「データ同期」ページでスクレイパーの設定と実行：
   - **クエリ**: 検索キーワード（デフォルト: `VOCALOID`）
   - **最大日数**: 過去N日以内の動画のみ取得（デフォルト: 365、空欄で無制限）
   - **検索対象**: `tags`、`tagsExact`、`title`、`description`、または`tags,title`のような組み合わせ
   - **カテゴリ**: カテゴリフィルタ（デフォルト: `MUSIC`）
   
   「同期開始」をクリックして動画データをダウンロード。1日1回の同期をおすすめします。

3. **検索・閲覧**: 検索バーでキーワード検索。タグフィルタで結果を絞り込み。ソート数式で再生数やマイリスト数などの重み付けを調整。「視聴済み除外」を有効にすると、既に見た動画を非表示にできます。

4. **動画視聴**: 動画をクリックすると埋め込みプレイヤーで再生。リストから連続自動再生。再生設定でオートスキップを有効にすると、動画のエンディングを自動スキップできます。

5. **PiPモード**: ポップアウトボタンでPiPウィンドウを開く。ナビゲーションのない簡易プレイヤーで、常に最前面に表示。

### スクレイパー設定オプション

| オプション | デフォルト | 説明 |
|-----------|-----------|------|
| `query` | `VOCALOID` | ニコニコSnapshot APIの検索キーワード |
| `max_age_days` | `365` | 過去N日以内の動画のみ取得。空欄で無制限 |
| `targets` | `tags` | 検索対象: `tags`、`tagsExact`、`title`、`description`、または組み合わせ |
| `category_filter` | `MUSIC` | ニコニコカテゴリフィルタ（MUSIC, GAME, ANIME, ENTERTAINMENT, DANCE, OTHER） |

---

## ソースからのビルド

### 前提条件

- Node.js 18+
- Rust 1.70+ (`cargo`含む)
- Windows 10/11 SDK

### ビルド手順

```bash
# リポジトリをクローン
git clone https://github.com/anton1615/VOCALOID-Search-Desktop.git
cd VOCALOID-Search-Desktop/vocaloid-search-desktop

# 依存関係をインストール
npm install
```

#### 開発ビルド

```bash
npm run tauri dev
```

フロントエンド開発サーバーとTauriが開発モードで起動します。

#### 本番ビルド

```bash
# フロントエンドとバックエンドを一緒にビルド
npm run tauri build

# またはデバッグフラグ付きでビルド（ビルドが高速、バイナリは大きい）
npm run tauri build -- --debug
```

> **重要**: Tauriは埋め込みプレイヤー用にHTTP localhostの代わりにカスタムプロトコル（`tauri://localhost`）を使用します。これはニコニコの埋め込みプレイヤーが`localhost`ドメインからのリクエストを拒否するためです。
>
> **開発時の注意**: ニコニコがlocalhost接続を拒否するため、`tauri dev`で埋め込みプレイヤーをテストできません。フロントエンドの変更をテストするには、アプリケーション全体を再ビルドする必要があります。`--debug`を使用するとRustの最適化をスキップしてビルド時間を大幅に短縮でき、開発サイクルがスムーズになります。正式な配布には`--debug`なしのリリースビルドを使用してください。

ビルドされた実行ファイルの場所：
```
vocaloid-search-desktop/src-tauri/target/release/vocaloid-search-desktop.exe
```

---

## 既知の問題と今後の計画

> ⚠️ **プロトタイプ版**: 早期プロトタイプです。機能が不安定で、テストされていない部分があります。

### 既知の問題

1. **履歴ページ**: 視聴記録は表示されますが、クリックしても動作しません
2. **PiP ↔ メインウィンドウ同期**: 状態同期が不完全 ✅ v1.0.1 で修正済み
   - ~~PiPでの再生が履歴に記録されません~~ PiPでの再生が正しく履歴に記録されるようになりました
   - ~~PiPを開く/閉じる際、再生状態がリセットされます~~ v1.1.0 で修正済み
3. **PiPウィンドウ**: まれに閉じられないことがあります（原因不明）
4. **PiPプレイリスト読み込み**: PiPが読み込み済み結果の最後に達すると、メインウィンドウがさらに読み込むのを待ちます（PiP自体は追加読み込みをトリガーできません） ✅ v1.1.0 で修正済み
5. **地域制限動画**: 自動再生が中断されます。再生できないため視聴済みにも記録されません
6. **イベント進行中のタブ切り替え**: イベント進行中（スクレイパー同期、メインウィンドウやPiPでの動画再生など）にタブを切り替えると、予期しない問題が発生する可能性があります
   - ~~検索ページに戻った時のUI状態の不整合~~ v1.1.2 で修正済み - SearchViewがRustから状態を復元するように
   - PiPウィンドウの同期失敗（稀、原因不明）
### 今後の計画

1. **「あとで見る」機能**: ニコニコの「あとで見る」のような機能を実装予定
2. **カスタムプレイリスト**: ユーザー定義の動画コレクション
3. **状態同期の改善**: メインウィンドウとPiP間の同期改善
4. **キーボードショートカット**: 再生コントロール用のグローバルキーボードショートカットを追加（再生/一時停止、前/次、音量など）
5. **ブラウザで開く**: 埋め込みプレイヤーと連携してリンクをデフォルトブラウザで開く、またはPiPウィンドウにボタンを追加して現在の動画のニコニコ元ページを素早く開く
6. **グローバル音量コントロール**: 埋め込みプレイヤーとは独立したアプリケーション全体の音量コントロールを追加
7. **データベースサイズ表示**: 同期ページに現在のデータベースサイズを表示
8. **データベースサイズ推定**: 同期前に検索条件に基づいてデータベースサイズを推定し、ユーザーがストレージを計画できるようにする
9. **ストレージ容量チェック**: 同期前にストレージ不足を警告する機能
10. **クリッカブルタグ**: タグをクリックして検索ボックスに検索条件として追加できるようにする
11. **タイトルと作者のリンク**: 埋め込みプレイヤー上部の動画タイトルと作者名をクリック可能にし、それぞれのニコニコ元ページをデフォルトブラウザで開けるようにする
12. **Linux対応**: Tauriのクロスプラットフォーム機能を活用したネイティブLinuxビルド
13. **オフライン再生**: インターネット接続がない場合に備えて動画をローカルにダウンロードしてオフラインで視聴できる機能
---

## ライセンス

[MIT License](./LICENSE)

---

## リリースノート

### v1.3.1 - WebView セキュリティ基盤と PiP 再生修正

**主な変更点:**
- Tauri WebView の CSP を無効化したままにせず、最小構成の baseline を追加
- 新しい baseline 下でも開発起動、パッケージ版ビルド、NicoNico 埋め込み再生の互換性を維持
- メインウィンドウと PiP の再生経路を揃え、両ウィンドウで埋め込み再生が安定するよう改善

**バグ修正:**
- 再レンダー後に PiP の埋め込み iframe がソース未設定のまま描画される回帰を修正
- iframe window を直接使えない状況で、load-complete 後の PiP 自動再生が失敗する問題を修正
- `shell:allow-open` の hardening は今回のスコープ外としつつ、今後の課題として追跡できるよう明記

### v1.3.0 - 再生設定の改善

**主な変更点:**
- メインウィンドウの常時表示オートスキップチェックボックスを、歯車から開く再生設定パネルに置き換え
- 自動再生とエンディング自動スキップを独立した設定として表示
- 設定パネルの開閉状態は保存せず、常に折りたたみ状態から開始
- 自動再生・自動スキップの設定値をアプリ再起動後も復元するよう改善

**バグ修正:**
- 次の動画へ切り替わった後に自動再生が始まらない問題を修正
- クロスオリジン iframe のメッセージターゲット解決が不安定で再生コマンドが失敗する問題を修正
- エンディング自動スキップ無効時でも、動画終了で次の動画へ進んでしまう問題を修正
- スキップしきい値の内部ロジックは維持したまま、ユーザー向け秒数入力 UI を削除

### v1.1.2 - バグ修正

**バグ修正:**
- 再生中にタブを切り替えた時のUI状態の不整合を修正 - SearchViewがRust AppStateから状態を復元するように（リセットされなくなりました）
- タブ切り替え時のPiP状態が復元されない問題を修正 - `pipActive`がRust AppStateと同期されるように
- メインウィンドウを閉じた時にPiPウィンドウが残る問題を修正 - PiPウィンドウはメインウィンドウと一緒に自動的に閉じられるように

### v1.1.1 - バグ修正

**バグ修正:**
- データベース新鮮度チェックのロジックを修正 - 上次更新時間を最近の6:00 JST閾値と正しく比較するように（以前は誤った24時間ウィンドウロジックを使用）

### v1.1.0 - アーキテクチャリファクタリング

**主な変更点:**
- **Rust統一状態管理**: すべてのアプリケーション状態がRust AppStateで管理され、フロントエンドコンポーネントは純粋な表示レイヤーとして機能
- **AppStateのSearchState**: 検索パラメータ（クエリ、フィルタ、ソート、ページ）が`AppState.search_state`に保存され、ウィンドウ間で一貫したアクセスが可能に
- **is_watched同期**: メインウィンドウとPiP間で視聴ステータスが明示的な同期ロジックなしで同期されるように
- **任意のウィンドウからloadMore可能**: PiPが結果の最後に達した際、直接`loadMore()`をトリガーでき、再生の中断を解消

**バグ修正:**
- データベース新鮮度チェックのロジックを修正 - 最新の6:00 JST閾値と正しく比較するように
- PiP再生の結果境界での途切れを修正 - 終了前にプリロードが開始されるように
- 連続再生中の自動スクロールを追加 - 現在再生中の動画が常に2つ下まで見える状態を維持

### v1.0.2 - バグ修正

**バグ修正:**
- 「視聴済み除外」フィルタが空の結果を返す問題を修正 - SQLテーブル参照を`watched`から`history`に修正

### v1.0.0 - 初回リリース

VOCALOID Search Desktopの初回リリース。主な機能:
- Spotify風のモダンUI（ライト/ダークモード対応）
- 多言語対応（英語、日本語、中国語繁体字）
- ピクチャー・イン・ピクチャー再生用PiPウィンドウ
- 視聴履歴の記録
- 視聴済み除外フィルタ
- ウィンドウ状態の保存
- FTS5全文検索対応のローカルSQLiteデータベース
- カスタム数式での並べ替え・フィルタリング
- オートスキップ機能
- ニコニコ埋め込みプレイヤーによる連続再生
- 無限スクロールページネーション